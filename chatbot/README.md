# Chatbot UI

React/TypeScript frontend for conversational interaction with the AI agents. Features Cognito authentication, agent selection toggle (LangGraph vs Strands), streamed response display, and conversation history.

Deployed to ECS Fargate.
