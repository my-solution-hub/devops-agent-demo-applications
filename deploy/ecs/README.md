# ECS Task Definitions

ECS Fargate task definitions and service configurations for containerized services (Feeding, Health Monitor) and React frontends (Chatbot UI, Device Simulator, Admin Console). Includes health checks and environment-specific configuration.
