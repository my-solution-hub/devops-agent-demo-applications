# Kubernetes Manifests

Kubernetes Deployment, Service, ConfigMap, and Ingress resources for EKS-deployed services (API Gateway, Cat Profile). Includes liveness/readiness probes and environment-specific configuration.
