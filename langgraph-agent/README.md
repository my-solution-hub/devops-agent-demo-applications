# LangGraph Workflow Agent

Python-based LangGraph workflow agent for cat-care conversation. Models the conversation as a directed graph with nodes for intent classification, cat profile lookup, device commands, RAG retrieval, response generation, and clarification.

Deployed to AgentCore Runtime.
