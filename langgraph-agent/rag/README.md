# RAG Pipeline

Retrieval-Augmented Generation pipeline embedded within the LangGraph agent. Handles document loading, chunking, embedding, vector storage (FAISS locally, Bedrock Knowledge Base in cloud), and similarity-based retrieval of cat-care domain knowledge.
