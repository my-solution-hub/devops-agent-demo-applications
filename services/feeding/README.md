# Feeding Service

Django (Python) service for managing feeding schedules and recording feeding events. Uses PostgreSQL (Django ORM) for schedules and DynamoDB for feeding events. Includes scheduled feeding automation with Celery.

Deployed to ECS Fargate.
