# Strands Multi-Agent System

Python-based multi-agent system using the Strands Agents SDK. Includes an Orchestrator Agent that routes requests to specialist agents (Feeding, Health, Device) for domain-specific cat-care tasks.

Deployed to AgentCore Runtime.
